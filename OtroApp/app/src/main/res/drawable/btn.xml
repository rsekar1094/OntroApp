<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"
    android:padding="@dimen/Layout_margin">
    <solid android:color="@color/RedBtn"
        />
    <corners
        android:bottomRightRadius="@dimen/Layout_margin"
        android:bottomLeftRadius="@dimen/Layout_margin"
        android:topLeftRadius="@dimen/Layout_margin"
        android:topRightRadius="@dimen/Layout_margin"/>

    <stroke
        android:width="2dp"
        android:color="@color/RedBtn"
        />
</shape>